name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
<<<<<<< HEAD
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '14' # 사용하고 있는 Node.js 버전으로 설정
      - run: npm install
      - run: npm run build
=======
      - run: npm clu & npm run build
>>>>>>> 184b3d7 (Save work in progress)
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_FLUTTERFORDEMOAPP_FA846 }}
          channelId: live
          projectId: flutterfordemoapp-fa846
